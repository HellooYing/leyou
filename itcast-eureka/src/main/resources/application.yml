server:
  port: 10086 # 端口
spring:
  application:
    name: eureka-server # 应用名称，会在Eureka中显示
eureka:
  client:
    service-url: # EurekaServer的地址，非集群是自己的地址，集群需要加上其它Server的地址。
      defaultZone: http://127.0.0.1:10086/eureka
      #,http://127.0.0.1:10087/eureka